
@model BookDetailsDto

@{
    ViewData["Title"] = "Details";

    string authors = String.Join(
        separator: ", ",
        values: Model.Authors
            .Select(a => $"{a.LastName} {a.FirstName}"));

    string tags = string.Join(
        separator: ", ",
        values: Model.Tags);

    string imagesPath = @"/uploads/images";
}

<div>
    <h4>@Html.DisplayFor(model => model.Title)</h4>
    <hr />
    <br />
    <dl class="dl-horizontal">
        <dd>
            <img class="img-fluid" src="@($"{imagesPath}/{Model.ImageUrl}")">
            <br />
            <a asp-action="EditImage"
               asp-controller="Home"
               asp-route-id="@Model.BookId"
               asp-area="contentmanager">edit</a>
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Authors)
        </dt>
        <dd>
            @authors
            <text> | </text>
            <a asp-action="EditAuthors"
               asp-controller="Home"
               asp-area="contentmanager"
               asp-route-id="@Model.BookId">
                edit
            </a>
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Tags)
        </dt>
        <dd>
            @tags
            <text> | </text>
            <a asp-action="EditTags"
               asp-controller="Home"
               asp-area="contentmanager"
               asp-route-id="@Model.BookId">
                edit
            </a>
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Price)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
            <text> | </text>
            <a asp-action="EditDescription"
               asp-controller="Home"
               asp-area="contentmanager"
               asp-route-id="@Model.BookId">
                edit
            </a>
        </dd>
    </dl>
    <br />
    <div class="text-center">
        <a asp-action="Index"
           asp-controller="Home"
           class="btn btn-secondary">back to catalog</a>
    </div>
</div>